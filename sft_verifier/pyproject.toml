[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "sft-verifier"
version = "0.1.0"
description = "Data preparation, LoRA SFT, and evaluation for the Text-to-SQL verifier"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

