# 프로젝트 제약사항

## 1. 문서의 목적과 적용 범위

이 문서는 본 프로젝트를 구현하거나 실험하는 모든 구성원과 코딩 에이전트가 공통으로 지켜야 할 제약사항을 정의합니다. 구체적인 모델 구조나 학습 방법을 규정하기보다, 어떤 구현을 선택하더라도 유지되어야 하는 실행 환경, 안전성, 재현성 및 평가 원칙을 다룹니다.

별도의 합의 없이 이 문서의 제약사항을 우회해서는 안 됩니다. 구현 편의를 위해 제약을 변경해야 한다면, 변경 이유와 실험 결과에 미치는 영향을 먼저 명시해야 합니다.

## 2. 로컬 개발과 서버 실행의 분리

- 코드 작성과 기본 검증은 로컬 macOS 환경에서 수행합니다.
- GPU가 필요한 학습, 전체 데이터셋 평가 및 최종 성능 측정은 Linux 서버에서 수행합니다.
- 로컬 환경에서의 실행 성공만으로 구현이 완료되었다고 판단하지 않습니다. 서버에서도 최소한 smoke test가 통과해야 합니다.
- 동일한 소스 코드를 별도로 수정하지 않고 로컬과 서버에서 실행할 수 있어야 합니다. 환경 차이는 설정 파일, CLI 인자 또는 환경 변수로 관리합니다.
- 운영체제, CPU 아키텍처, GPU 유무에 따라 달라지는 동작을 최소화해야 합니다. 플랫폼별 처리가 불가피하다면 이를 명시적으로 분리하고 문서화합니다.
- 절대 경로, 사용자 홈 디렉터리, 특정 서버 주소, 특정 GPU 번호 등을 소스 코드에 직접 작성하지 않습니다.
- 데이터, 모델, 캐시, checkpoint 및 결과 저장 경로는 외부에서 설정할 수 있어야 합니다.
- device, dtype, batch size, worker 수 등 실행 환경에 영향을 받는 값은 변경 가능해야 합니다.
- 다만 현재 합의된 초기 single-turn smoke test는 확인된 서버를 대상으로 `cuda:0` 단일 GPU, FP32, eager attention, batch size 1을 재현성 계약으로 고정합니다. 이후 모델·서버 실험으로 일반화할 때에는 이 smoke 전용 검증을 별도 profile로 분리해야 합니다.
- Python과 주요 라이브러리의 버전을 고정하고, 로컬과 서버에서 동일한 의존성 정의를 사용합니다. 플랫폼별 의존성이 필요하다면 그 차이를 명확히 기록합니다.
- 학습 및 평가 명령은 사용자 입력을 기다리지 않는 비대화형 방식으로 실행할 수 있어야 합니다.

## 3. 서버 및 연산 자원

- GPU 개수, VRAM 크기, CPU 코어 수 등 특정 서버 사양을 코드에서 전제로 삼지 않습니다.
- 위 일반 원칙의 예외로, 초기 Qwen smoke profile의 server doctor는 현재 제공된 TITAN Xp(compute capability 6.1, 12GB)에서 검증된 software 조합인지 확인합니다. 이는 프로젝트 전체 학습 코드가 해당 GPU에 종속된다는 의미가 아닙니다.
- 연산 자원과 관련된 설정은 가능한 한 외부에서 조정할 수 있어야 합니다.
- 장시간 학습은 중단 후 재개할 수 있도록 checkpoint 저장과 resume를 지원해야 합니다.
- 전체 평가의 다중 GPU 추론은 GPU별 독립 process와 명시적인 data sharding으로
  수행할 수 있습니다. 이때 shard의 누락·중복을 검사하고 원래 dataset 순서를
  복원하며, 중간 generation 결과를 저장해 중단 후 재개할 수 있어야 합니다.
- 실험에는 최대 step, 최대 실행 시간 또는 이에 준하는 종료 조건을 두어야 합니다.
- checkpoint, 모델 가중치, 데이터베이스, 캐시 및 대용량 로그는 Git에 포함하지 않습니다.
- 실행 중 필요한 데이터나 모델을 암묵적으로 다운로드하지 않습니다. 필요한 리소스와 준비 방법, 저장 위치를 명시합니다.
- 서버 실행 결과에는 사용한 설정, 모델 및 데이터 버전, 실행 환경을 확인할 수 있는 정보를 남깁니다.

## 4. Spider 1.0 데이터 및 평가 환경

- 사용하는 Spider 1.0의 버전과 train/development/test split을 고정하고 기록합니다.
- 데이터베이스와 평가 데이터의 위치는 설정으로 관리하며 코드에 절대 경로를 넣지 않습니다.
- 학습 과정에서 development/test의 정답 SQL이나 평가 결과가 모델 입력에 유출되지 않도록 합니다.
- 모델 또는 agent가 접근할 수 있는 정보의 범위를 실험 전에 정의합니다. 데이터베이스 스키마, 컬럼 정보, 데이터베이스 내용 및 이전 SQL 실행 결과의 사용 여부는 동일한 실험군에서 일관되어야 합니다.
- 서로 비교하는 방법은 동일한 데이터 split과 평가 조건을 사용해야 합니다.
- 정확도는 Spider의 현재 공식 지표인 Test Suite Accuracy와 기존 Original Exact Set Match를 구분하여 산출합니다. 사용하는 공식 evaluator 소스와 test-suite database bundle의 버전 및 hash를 고정합니다.
- 단일 원본 데이터베이스에서 직접 비교한 실행 결과 일치율은 파이프라인 진단용 보조 지표로만 사용하며, Test Suite Accuracy라고 부르지 않습니다.
- 별도의 정확도 지표를 사용한다면 공식 지표와 이름 및 집계를 명확히 구분하여 보고합니다.
- SQLite 버전과 주요 실행 조건을 기록하여 환경 차이에 따른 결과 변화를 추적할 수 있어야 합니다.
- 데이터 원본을 직접 수정하지 않습니다. 전처리 결과나 smoke test용 부분집합은 별도의 위치에 생성합니다.

## 5. Smoke test 환경

Smoke test의 목적은 모델의 최종 성능을 측정하는 것이 아니라, 입력 처리부터 SQL 생성, 실행, 결과 수집까지의 전체 파이프라인이 정상적으로 연결되어 있는지 빠르게 확인하는 것입니다.

- 로컬과 서버에서 동일한 CLI·설정 계약과 고정 입력을 사용해야 합니다. 다만 로컬은
  `mock`, 서버는 `hf` backend를 명시적으로 선택하며, 이 차이는 실행 기록에 남깁니다.
- smoke test는 Spider 1.0의 소수 데이터베이스와 질의만 사용하며, 수 분 이내에 종료되는 것을 원칙으로 합니다.
- 전체 학습이나 고성능 GPU 없이도 실행할 수 있어야 합니다. 실제 모델이 필요한 경우에는 작은 모델, mock 또는 사전에 준비된 SQL을 사용하여 나머지 파이프라인을 검증할 수 있어야 합니다.
- 단순 조회뿐 아니라 JOIN, 집계, 정렬 및 중첩 질의 등 주요 SQL 유형을 일부 포함하는 것이 좋습니다.
- coder가 생성한 SQL을 실제 SQLite 데이터베이스에서 실행하고, 실행 성공 여부와 반환 결과를 확인해야 합니다.
- 필요한 경우 정답 SQL도 동일한 데이터베이스에서 실행하여 예측 SQL의 결과와 비교할 수 있어야 합니다.
- SQL 실행 결과뿐 아니라 오류 유형, 오류 메시지 및 실행 시간을 기록해야 합니다.
- 질문별 예측 SQL의 실행시간은 실제 SQLite query 실행 및 결과 수집 구간을 고해상도 단조 시계로 측정하고, 원시 단위와 측정 경계를 함께 기록해야 합니다.
- smoke test 실패를 숨기거나 임의의 성공 결과로 대체해서는 안 됩니다.
- smoke test 통과는 전체 benchmark의 정확도나 최종 성능을 보장하지 않으며, 공식 평가를 대체하지 않습니다.

## 6. 생성 SQL의 실행 안전성

모델이 생성한 SQL은 신뢰할 수 없는 입력으로 취급합니다.

- Spider 데이터베이스는 읽기 전용으로 열거나, 원본과 분리된 복사본에서 실행해야 합니다.
- 평가에 필요한 읽기 질의만 허용합니다. `INSERT`, `UPDATE`, `DELETE`, `DROP`, `ALTER`, `ATTACH` 등 데이터나 실행 환경을 변경할 수 있는 명령은 차단해야 합니다.
- 장시간 또는 무한 실행을 방지하기 위해 모든 생성 SQL에 timeout을 적용해야 합니다.
- 가능하면 SQL 실행을 별도 프로세스에서 격리하여, timeout이나 비정상 종료가 전체 학습·평가 프로세스를 중단시키지 않도록 합니다.
- 반환 row 수, 출력 크기 및 로그 크기에 적절한 상한을 두어야 합니다.
- SQL 구문 오류, 실행 오류, timeout 및 빈 결과를 서로 구분하여 기록합니다.
- 실행 실패를 빈 결과나 정상 결과로 간주하는 silent fallback을 사용하지 않습니다.

## 7. 평가의 공정성과 재현성

- baseline, 제안 방법 및 ablation은 동일한 데이터 split과 평가 조건에서 비교합니다.
- Multi-turn reasoning, agentic workflow 및 강화학습 기반 fine-tuning의 효과를 분리해서 확인할 수 있어야 합니다.
- 비교 실험에서는 모델, 데이터, 입력 정보, 추론 예산 등의 조건을 가능한 한 동일하게 유지합니다. 조건이 다르다면 차이와 그 이유를 결과에 함께 기록합니다.
- random seed, 모델 버전, 데이터 버전, 주요 hyperparameter 및 실행 명령을 보존합니다.
- 최종 결과뿐 아니라 모델의 원본 출력, 생성 SQL, 실행 오류 및 timeout도 분석 가능한 형태로 저장합니다.
- execution accuracy와 SQL 구조 기반 지표를 구분하여 기록합니다.
- 단일 데이터베이스에서 두 SQL의 실행 결과가 같더라도 의미적으로 동일한 SQL임을 항상 보장하지는 않는다는 점을 고려합니다.
- 예측 SQL의 query 실행시간, worker 처리시간, parent가 관찰한 전체 wall-clock 시간은 서로 다른 값으로 보존합니다. 향후 학습에 사용할 실행시간 관측값에는 공식 evaluator 실행시간이나 모델 생성시간을 섞지 않습니다.
- GPU별 generation은 병렬화할 수 있지만, 비교 가능한 SQL 실행시간을 수집하는
  평가에서는 generation이 끝난 뒤 하나의 CPU 평가 process가 prediction을 먼저,
  gold를 나중에 순차 실행합니다. GPU worker의 동시 CPU·DB 작업이 query timing에
  섞이지 않도록 합니다.
- 정확도와 실행시간은 향후 강화학습 fine-tuning에 사용할 수 있는 원시 평가값으로 저장하되, 별도의 합의 전에는 reward 결합식이나 가중치를 구현하지 않습니다.
- 통계적 유의성을 주장할 경우 반복 실험 횟수와 검정 방법을 함께 명시합니다.

## 8. 설정, 저장소 및 비밀정보 관리

- 모든 소스 코드, 실행 스크립트, 설정 및 테스트 구현은 프로젝트 루트의 `./code/` 아래에 둡니다.
- 비밀번호, API key, access token, 개인 서버 주소 등 민감한 정보를 소스 코드나 버전 관리 대상 파일에 포함하지 않습니다.
- 환경별 설정은 환경 변수 또는 버전 관리에서 제외된 로컬 설정으로 주입합니다.
- 재현에 필요한 비민감 설정 파일, 의존성 정의 및 실행 명령은 버전 관리합니다.
- 소스 코드, 원본 데이터, 전처리 데이터, checkpoint 및 실험 결과가 서로 섞이지 않도록 저장 위치를 구분합니다.
- 중요한 설정이나 파일이 누락된 경우에는 명확한 오류를 발생시킵니다. 결과를 바꿀 수 있는 임의의 기본값이나 silent fallback을 사용하지 않습니다.
- 생성 파일과 대용량 artifact가 실수로 커밋되지 않도록 `.gitignore`를 관리합니다.

## 9. 구현 완료의 최소 기준

기능 구현은 다음 조건을 모두 만족해야 완료된 것으로 간주합니다.

1. 로컬 macOS 환경에서 관련 단위 테스트 또는 기본 검증이 통과합니다.
2. 로컬의 Spider 1.0 smoke test에서 SQL 생성·실행·결과 수집 과정이 정상적으로 동작합니다.
3. 동일한 코드와 실행 인터페이스가 Linux 서버에서도 동작합니다.
4. 서버 smoke test가 통과하며, GPU가 필요한 기능은 서버에서 별도로 검증됩니다.
5. 실행 방법과 필수 설정이 문서화되어 있습니다.
6. 생성 SQL이 읽기 전용, timeout 및 오류 기록 원칙을 준수합니다.
7. 실험을 재현하는 데 필요한 설정과 실행 기록이 보존됩니다.
